[phases.setup]
nixPkgs = ["nodejs-22_x"]

[phases.install]
cmds = ["cd server && npm install", "cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "cd server && node server.js"
